# Hyperliquid Trading Bots - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# HYPERLIQUID API CONFIGURATION
# =============================================================================

# Your Hyperliquid private key (KEEP THIS SECRET!)
# This is used to sign transactions and authenticate with the Hyperliquid API
# Format: 64-character hexadecimal string (without 0x prefix)
HYPERLIQUID_PRIVATE_KEY=your_private_key_here_64_characters_long

# Your wallet address (derived from the private key above)
# This should match the address that owns your Hyperliquid account
# Format: 42-character hexadecimal string starting with 0x
WALLET_ADDRESS=0xYourWalletAddressHere

# =============================================================================
# TRADING CONFIGURATION (Optional - can be set in bot files)
# =============================================================================

# Default asset to trade (asset index on Hyperliquid)
# SOL = 5, ETH = 0, BTC = 1, etc.
DEFAULT_ASSET=5

# Default leverage multiplier
DEFAULT_LEVERAGE=20

# Default position size in USD
DEFAULT_POSITION_SIZE=300

# =============================================================================
# SAFETY SETTINGS
# =============================================================================

# Set to 'true' to enable live trading, 'false' for paper trading mode
LIVE_TRADING=false

# Maximum daily loss limit in USD (bot will stop if exceeded)
MAX_DAILY_LOSS=1000

# =============================================================================
# EXPERIMENTAL COLOR TRADER SETTINGS
# =============================================================================

# Screen region for color detection (x,y,width,height)
# These will be set during the calibration process
SCREEN_REGION_X=100
SCREEN_REGION_Y=100
SCREEN_REGION_WIDTH=200
SCREEN_REGION_HEIGHT=200

# Target colors for trading signals (RGB values)
GREEN_TARGET_R=76
GREEN_TARGET_G=175
GREEN_TARGET_B=80

RED_TARGET_R=241
RED_TARGET_G=147
RED_TARGET_B=65

# Color matching tolerance (0-255, higher = more tolerant)
COLOR_TOLERANCE=80

# =============================================================================
# API ENDPOINTS (Usually don't need to change)
# =============================================================================

# Hyperliquid API endpoints
HYPERLIQUID_API_URL=https://api.hyperliquid.xyz
HYPERLIQUID_INFO_URL=https://api.hyperliquid.xyz/info

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env: cp env.example .env
# 2. Fill in your HYPERLIQUID_PRIVATE_KEY and WALLET_ADDRESS
# 3. Adjust trading parameters as needed
# 4. Set LIVE_TRADING=true when ready for real trading
# 5. NEVER commit your .env file to version control!